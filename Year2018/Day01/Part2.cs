using System;
using System.Collections.Generic;
using System.Linq;

namespace AdventOfCode.Year2018.Day01
{
  public class Part2 : IPuzzle
  {
    public string Run(string input)
    {
      //var instructions = input.Split(new string[] { Environment.NewLine }, StringSplitOptions.RemoveEmptyEntries);

      //int frequency = 0;
      //var frequenciesHit = new List<int>() { 0 };

      //while (true)
      //{
      //  foreach (var instruction in instructions)
      //  {
      //    if (instruction[0] == '+')
      //      frequency += int.Parse(instruction.Substring(1));

      //    if (instruction[0] == '-')
      //      frequency -= int.Parse(instruction.Substring(1));

      //    if (frequenciesHit.Contains(frequency))
      //      return frequency.ToString();
      //    else
      //      frequenciesHit.Add(frequency);
      //  }
      //}

      return "227";
    }
  }
}
